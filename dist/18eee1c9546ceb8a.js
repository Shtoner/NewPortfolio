import*as THREE from"three";import"./stillies.css";import Parallax from"parallax-js";var scenes=document.getElementById("body"),parallaxInstance=new Parallax(scenes,{relativeInput:!0});const blok=document.getElementsByClassName(".blok");console.log(blok);const canvas=document.querySelector("canvas.webgl"),scene=new THREE.Scene,sizes={width:window.innerWidth,height:window.innerHeight},camera=new THREE.PerspectiveCamera(75,sizes.height/sizes.width);camera.position.xyz=1;const mat=new THREE.MeshStandardMaterial({color:16776960}),particlesGeometry=new THREE.BufferGeometry,count=1e3,positions=new Float32Array(3e3);for(let e=0;e<3e3;e++)positions[e]=10*(Math.random()-.5);particlesGeometry.setAttribute("position",new THREE.BufferAttribute(positions,3));const particlesMaterial=new THREE.PointsMaterial;particlesMaterial.size=.02,particlesMaterial.sizeAttenuation=!0,particlesMaterial.color="black";const particles=new THREE.Points(particlesGeometry,particlesMaterial);particles.position.y=20,particles.position.z=50,particles.scale.x=6,particles.scale.y=8,particles.scale.z=-3,console.log(particles),scene.add(camera,particles);const renderer=new THREE.WebGLRenderer({canvas});renderer.setClearAlpha(.03),renderer.setSize(sizes.width,sizes.height);const mouse=new THREE.Vector2,target=new THREE.Vector2,windowHalf=new THREE.Vector2(sizes.width/2,sizes.height/2);function onMouseMove(e){mouse.x=e.clientX-windowHalf.x,mouse.y=e.clientY-windowHalf.x,e.pageX,e.pageY}function onResize(e){const t=window.innerWidth,a=window.innerHeight;windowHalf.set(t/2,a/2),camera.aspect=t/a,camera.updateProjectionMatrix(),renderer.setSize(t,a)}function animate(){requestAnimationFrame(animate),target.x=2e-4*(1-mouse.x),target.y=2e-4*(1-mouse.y),camera.rotation.x+=.05*(target.y-camera.rotation.x),camera.rotation.y+=.05*(target.x-camera.rotation.y),renderer.render(scene,camera)}document.addEventListener("mousemove",onMouseMove,!1),window.addEventListener("resize",onResize,!1),camera.position.set(0,20,100),animate(),renderer.render(scene,camera);